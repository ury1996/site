---
import Logo from './Logo.astro';

const links = [
  { href: '#diagnostico', texto: 'Diagnóstico' },
  { href: '#como-funciona', texto: 'Como funciona' },
  { href: '#depoimentos', texto: 'Resultados' },
  { href: '#faq', texto: 'Dúvidas' },
];
---

<header class="topo">
  <div class="container topo-inner">
    <a href="#inicio" class="logo" aria-label={`${'Sites Rankly'} — início`}>
      <Logo width={132} height={64} priority />
    </a>

    <nav aria-label="Navegação principal">
      {links.map((link) => <a href={link.href}>{link.texto}</a>)}
    </nav>

    <a class="btn btn-sol btn-topo" href="#proposta">Proposta grátis</a>
  </div>
</header>

<style>
  .topo {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(10, 24, 48, 0.92);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .topo-inner {
    display: flex;
    align-items: center;
    gap: 28px;
    padding-block: 12px;
  }

  .logo {
    display: flex;
    align-items: center;
    text-decoration: none;
    background: #fff;
    border-radius: 12px;
    padding: 5px 12px;
  }

  .logo :global(img) {
    width: auto;
    height: 46px;
  }

  nav {
    display: flex;
    gap: 22px;
    margin-left: auto;
  }

  nav a {
    color: rgba(255, 255, 255, 0.78);
    text-decoration: none;
    font-size: 0.92rem;
    font-weight: 500;
  }

  nav a:hover {
    color: var(--sol);
  }

  .btn-topo {
    padding: 10px 20px;
    font-size: 0.9rem;
  }

  @media (max-width: 820px) {
    nav {
      display: none;
    }

    .topo-inner {
      justify-content: space-between;
    }
  }
</style>
